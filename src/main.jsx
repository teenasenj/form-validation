import React from 'react'
import ReactDOM from 'react-dom/client'
import From_validation from './Form_validation'



ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
  
    <From_validation/>
  </React.StrictMode>,
)
